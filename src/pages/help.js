import React from 'react'
import { Layout } from 'components/Layout'
import withProvider from 'withProvider'

const Help = () => {
  return (
    <Layout>
      Help
    </Layout>
  )
}

export default withProvider(Help)
